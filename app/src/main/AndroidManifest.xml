<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itla.mudat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.mudaT">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.mantenimiento.RegistroUsuario" />
        <activity android:name=".view.Usuarios" />
        <activity android:name=".view.Anuncios" />
        <activity android:name=".view.mantenimiento.RegistroAnuncio" />
        <activity android:name=".view.mantenimiento.RegistroCategoria" />
        <activity android:name=".view.Categorias"></activity>
    </application>

</manifest>